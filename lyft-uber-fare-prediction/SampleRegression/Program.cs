
// This file was auto-generated by ML.NET Model Builder. 

using System;

namespace SampleRegression.ConsoleApp
{
    class Program
    {
        static void Main(string[] args)
        {
            if(args.Length < 3)
            {
                Console.WriteLine("Please specify distance, cab type, and time stamp");
                return;
            }

            var distance = float.Parse(args[0]);
            var cab_type = args[1];
            var time_stamp = float.Parse(args[2]);
            SampleRegression.ModelInput sampleData = new SampleRegression.ModelInput()
            {
                Distance = distance,
                Cab_type = cab_type,
                Time_stamp = time_stamp
            };

            // Make a single prediction on the sample data and print results
            var predictionResult = SampleRegression.Predict(sampleData);

            Console.WriteLine($"\n\nPredicted Price: {predictionResult.Score}\n\n");
            Console.WriteLine("=============== End of process, hit any key to finish ===============");
            Console.ReadKey();
        }
    }
}
